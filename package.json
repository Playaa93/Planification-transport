{
  "name": "transportpro-static",
  "version": "1.0.0",
  "private": true,
  "description": "TransportPro - site statique servi par Netlify",
  "scripts": {
    "start": "echo 'Static site (no build). Use a local server for dev.'"
  }
}
